version: '3.8'

services:
  # Frontend (PWA)
  frontend:
    build: 
      context: ./client
      dockerfile: Dockerfile
    container_name: hotel-frontend
    ports:
      - "3000:80"
    restart: unless-stopped
